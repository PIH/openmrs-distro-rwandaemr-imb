<htmlform 
	htmlformUuid="06fd8b9c-52bc-4afd-9f28-72afd51a633b" 
	formUuid="7f706bb0-8f1d-4fa8-acfc-26fb6e188d45" 
	formName="Oncology - Patient Labels for Printing" 
	formDescription="Avery 5163 style patient labels" 
	formVersion="0.1" 
	formEncounterType="b1c2d207-7221-4c23-b580-6c5de5385847" 
	formPublished="true" 
	formRetired="false"
>
	<!-- Autogenerated example form  (template from 01-Nov-2010 -->
	<macros>
		paperFormId = (Fill this in)
		headerColor =#009d8e
		fontOnHeaderColor = white
	</macros>

	<style>
		.section {
			border: 1px solid $headerColor;
			padding: 2px;
			text-align: left;
			margin-bottom: 1em;
			font-size: 20px;
		}
		.sectionHeader {
			background-color: $headerColor;
			color: $fontOnHeaderColor;
			display: block;
			padding: 2px;
			font-weight: bold;
		}
		table.baseline-aligned td {
			vertical-align: baseline;
			font-size: 20px;
		}
		#printArea {
        display: none;
		font-size: 120px;
        }
	</style>

	<span style="float:right">Paper Form ID: $paperFormId</span>
	

<h2>Oncology - Patient Labels (v1.0)</h2>

	<section headerLabel="IGNORE THIS part. Just print the second page">
		<table class="baseline-aligned">
			<tr>
				<td>Date:</td>
				<td><encounterDate default="today"/></td>
			</tr>
			<tr>
				<td>Location:</td>
				<td><encounterLocation/></td>
			</tr>
			<tr>
				<td>Provider:</td>
				<td><encounterProvider/></td>
			</tr>
		</table>
	</section>

	<submit/>

<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/><br/><br/>
<br/><br/>
<table>
<tr height="210">
<td>
	
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
<td>
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
</tr>
<tr height="210">
<td>
	
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
<td>
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
</tr>
<tr height="210">
<td>
	
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
<td>
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
</tr>
<tr height="210">
<td>
	
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
<td>
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
</tr>
<tr height="210">
<td>
	
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
<td>
		<table class="baseline-aligned">
			<tr>
				<td><lookup class="value" expression="patient.personName"/>
				|
				<lookup class="value" complexExpression="#if( \$patient.birthdateEstimated ) ~#end"/> <lookup class="value" expression="patient.birthdate"/>	
				</td>
			</tr>
			<tr>
				<td><lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('IMB ID') ) $patId #end "/>
			|
				
				<lookup class="value" complexExpression="#foreach( $patId in $patientIdentifiers.get('OpenClinic ID') ) $patId #end "/></td>
			</tr>
		<tr>
		<td>
		<lookup expression="patient.personAddress.countyDistrict" />
		|
		<lookup expression="patient.personAddress.cityVillage" />
		|
		<lookup expression="patient.personAddress.address3" />
		|
		<lookup expression="patient.personAddress.address1" />
		</td>
	</tr>
		</table>
</td>
</tr>
</table>



</htmlform>